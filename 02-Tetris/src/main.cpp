#include "etc/env"
#include "lib/CanvasMonocrome.cpp"
int main()
{
    /*
    CanvasMonocrome *canvas = new CanvasMonocrome(48, 48);
    colorGraphic(RED, MAGENTA);
    canvas->Clear();
    canvas->Draw(1, 1);
    colorGraphic(BLUE, CYAN);
    DrawFrame(3, 2, 10, 5);
    resetColor();
    printf("\n\n\n");
    */
    return 0;
}